# LangGraph / LangChain core
langgraph>=0.2.0
langchain>=0.2.0
langchain-anthropic>=0.1.0
langchain-community>=0.2.0

# LLM backends
anthropic>=0.30.0

# Alpaca trading (official v2 SDK)
alpaca-py>=0.20.0

# Market data
yfinance>=0.2.40
pandas-ta>=0.3.14b
pandas>=2.1.0
numpy>=1.26.0

# Macro data (FRED)
fredapi>=0.5.1

# Market calendar / scheduling
pandas_market_calendars>=4.3.0
APScheduler>=3.10.0
pytz>=2024.1

# Persistence (LangGraph SQLite checkpointer)
sqlalchemy>=2.0.0

# Observability
structlog>=24.1.0

# Utilities
python-dotenv>=1.0.0
httpx>=0.27.0       # async HTTP for Ollama REST
pydantic>=2.0.0
tenacity>=8.2.0     # retry logic for LLM API calls
